'use client'

import { ScrollingFish } from '@/components'

const TestPage = () => {
  return (
    <div className="min-h-screen overflow-hidden flex flex-col items-center justify-center p-8 pt-20 relative">
      <ScrollingFish />
    </div>
  )
}

export default TestPage
